.git/pr/
.grepai/
# The hub's runtime state in a project it manages: its db, agent homes (which hold
# live Claude credentials), sockets. Only the project config belongs in the repo.
.sindri/*
!.sindri/config.yaml
bin/
dist/
gh-local/gh
.td-root
.worktrees/
.todos/
CLAUDE.md
/gh
# Claude Code's own session state (sessionId, pid, acquiredAt) — a live lock for
# whichever agent session is running here, not feature code.
.claude/scheduled_tasks.lock
