# Tasks

- [x] 1. Add `e:edit` to the work-list help string in `viewList`
- [x] 2. Regenerate every list-view golden (help-bar string change)
- [x] 3. `view-work-list`: ADD requirement that the help bar lists every binding the list view accepts
- [x] 4. `go test ./...` + `sindri lint all` + `openspec validate help-bar-explains-every-hotkey --strict` pass
